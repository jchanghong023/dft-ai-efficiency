# 实施与验证记录

## 实施范围

- Python 3.11+ 同步、无状态二进制更新、静态门户构建、验证和公共 ZIP 打包。
- 2 项自动/显式可调用 Skill，3 个原生 Worker，`/code-and-verify`。
- 177 页公共桌面离线门户，分为开始使用、团队能力、维护手册、内部文档、DFT知识五个独立分区；95 个 TUI 注册命令、43 个 CLI 入口、5 个内置 Agent、1 个内置模板与 2 个模块命令。
- 内部文档单独输出到 `yellow/.site/`，默认最多 3000 行；本地 341 份测试 Markdown 中生成 283 份，跳过 58 份。公共手册在提交前检查暂存区，超限须拆页，不在构建时过滤。
- DFT 分区含 17 页、16 个本地 JavaScript 交互教学模型，按测试原理、扫描与时钟、压缩与功耗、访问与分层、内建自测试、工程落地六条路线组织；在已有 Scan、EDT 基础上覆盖全部 14 个核心缺项。
- `dist/` 仅两个 Linux 实体；发行方 SHA-256 在下载时验证，不保存版本和校验状态。

## 验证环境及边界

Windows Python 3.13 用于构建与浏览器测试；WSL Ubuntu 24.04 / Python 3.12 用于 Linux 同步逻辑和 /proc 集成。测试目录全部位于 `.tmp/`，不操作真实个人 OMP 配置。桌面浏览器为 Playwright Chromium，1440×1050；按用户要求不做小屏幕专项验收。

## 历史检查项目

以下保留此前检查记录，其中 AST 提取器、dist 版本文件与离线发行方校验状态已随本次简化移除。

2026-09-05 桌面视觉调整后，已重新构建网站并通过 `tests/verify.py`、公共 Markdown 行数检查及差异检查。调整包括首页卡片布局、DFT 学习入口、分区首页、导航和阅读排版。本次浏览器工具的安全策略拒绝访问 `file://`，因此没有进行调整后的浏览器验收；下表的浏览器结果及 `.tmp/browser-check/` 截图是调整前的记录，不能代表本次视觉效果已经验收。未改用其他浏览器入口或服务器绕过限制。

同日完成 DFT 核心内容扩展：6 个独立子代理分工实施，另设只读模型审查。最终重新构建 177 页公共内容和 283 页内部测试文档，7 个新增测试模块共 38 项通过；这些是 Node 纯模型、静态课程及资源检查，不是浏览器交互或公司硬件验证。内部页面隔离与资源链接的 3 项相关回归通过。30 份公共 Markdown 均不超过 3000 行。

模型回归覆盖真实布尔故障枚举、覆盖率分母、LOC/LOS 与 SE 时序、OCC 门控、scan hold/lockup、TAP 指令与 DR 路径、SIB 更新、Wrapper 模式、March 检测与冗余不足、LFSR/MISR 已知向量、压缩约束与 X/mask、逐周期翻转计数及基于证据的流程推进。均明确限定为教学模型。

| 检查 | 结果 |
|---|---|
| `tests/test_sync.py` | Windows 隔离逻辑检查通过；Linux 全部场景通过，包括真实 /proc 进程识别与 SIGTERM、符号链接拒绝、任意 cwd 与空格路径 |
| 同步异常分支 | 缺失/空/损坏数据库、源/目标 WAL、进程不退出、权限失败、LFS 指针、校验值错误均拒绝相应覆盖；个人文件保留 |
| `tests/test_metadata.py` | 4 项通过：跨文件数组与生成式注册、未知表达式失败、新项缺说明失败、公共读取与打包不访问 yellow |
| 源码快照复核 | 与固定标签 commit 的 95 个 TUI、43 个 CLI、5 个 Agent、1 个模板、2 个模块及工具清单一致；Fork 工作树无修改 |
| `tests/verify.py` | 177 页链接、片段、资源、全文搜索覆盖、公共输入哈希与两份实体 SHA-256 通过；检查全部本地 CSS/JS 的运行时外部资源引用 |
| 新增 DFT 模型与课程测试 | `test_dft_curriculum`、`test_dft_faults`、`test_dft_clocks`、`test_dft_access`、`test_dft_bist`、`test_dft_compression_power`、`test_dft_flow`：38 项通过；6 份新模型脚本语法通过 |
| `tests/test_portal.py` | 1440×1050 桌面 file:// 导航、全部内页加载、中文/命令/Skill 搜索、复制字节、禁用 JS 阅读通过；无外部资源请求及脚本错误 |
| `tests/test_internal_docs.py` | 8 项通过：默认及自定义阈值、2999/3000/3001 行、换行符、非法参数、旧页面与索引清理、内部链接与图片、公共数据隔离和桌面浏览 |
| `tests/test_doc_checks.py` | 3 项通过：3000/3001 行、暂存区与工作区差异、黄区排除、隔离仓库真实 pre-commit 成功/失败、其他钩子保留 |
| `tests/test_dft_portal.py` | 4 项通过：五个独立分区及各自导航；Scan 移位/捕获/正常与故障响应/播放暂停/重置；EDT 单错误与抵消；禁用 JS 静态阅读。无外部资源请求或脚本错误 |
| 公共提交钩子 | 已在当前仓库安装；检查 `portal/content/` 的暂存内容，默认最多 3000 行。未创建真实仓库提交 |
| Skill 格式 | 两项通过 skill-creator quick_validate；Windows 使用 UTF-8 模式 |
| 原生二进制 smoke | WSL 隔离 HOME 下实际输出 OMP `18.1.9+fork.170`、Claude Code `2.1.236`；未使用模型服务 |
| Git 分发规则 | 二进制 LFS 属性已配置；yellow 内部路径及 .tmp 被忽略；未提交或推送 |

运行命令详见 README。桌面截图和浏览器机器可读结果位于 `.tmp/browser-check/`，不进入公共包。公共发布由 `scripts/maintenance/package.py` 在产出前执行完整校验，再检查 ZIP CRC、成员白名单和包内二进制哈希；对应结果保存在发布包旁的 manifest 中。

## 页面视觉分层调整（2026-09-05）

- 独立首页模板和首页专属 CSS/JS：抽象数据流持续动效、暂停开关、减少动态效果及页面隐藏暂停；首页正文不推荐内部文档和 DFT 内容，顶部保留完整六项导航。
- 文档页：约 16px 正文、1.82 行高、桌面右侧页内目录跟随；当前模块搜索、左侧独立滚动与标题筛选；代码语言标识、基础词法着色、复制文本保持、图片放大。
- DFT：实验先于解释正文，扩大画布；缩放与选中读数不推进模型；四个连续播放实验增加 0.5×/1×/2× 速度，单步与播放仍使用相同状态计算。
- 已重建 177 页公共页面与 283 页内部测试文档。44 项相关回归通过，包括 38 项原课程/模型检查、3 项页面呈现检查、3 项黄区隔离及链接检查。资源完整性、公共 Markdown 行数检查通过。
- 纯 DOM 检查通过 `linkedom` 执行，不启动浏览器：覆盖首页暂停、模块搜索范围、代码文本保持、目录筛选、16 个实验初始化/单步/复位/画布控件、4 个播放速度及 Scan/LBIST 末值。该模拟不验证 CSS 布局、浏览器 API 的完整实现或实际视觉效果。
- 本次仍未进行浏览器视觉验收；此前浏览器工具安全策略拒绝 `file://` 访问，未使用其他入口或服务器绕过。历史浏览器截图不能作为新版验收结果。

## 持续完善：搜索、命令说明与验收脚本（2026-09-05）

- 三个浏览器验收入口已按当前六项顶部导航、模块搜索和明确的故障选择器更新；仅更新代码与语法检查，未运行浏览器，也未用其他入口绕过此前访问限制。
- 搜索改为可继续显示所有结果，公共与内部页面分别建立本地章节索引；跳转带本地查询及章节锚点。正文高亮跨行内标签和代码着色片段匹配，保留原文、代码复制字节及电路控件状态；支持上一处/下一处与清除。模型/DOM 检查覆盖结果超过 40 条、换词后重置、模块隔离、Unicode 偏移和代码保持。
- 43 个 CLI 入口新增人工复核的主要参数、使用条件和具体示例，来源为只读固定版命令实现。`cli-usage.json` 记录版本、commit、引用文件哈希；构建拒绝缺项和过期版本；维护入口另核对引用源码哈希。未执行示例中的模型调用、上传、服务启动或写入操作。
- 原设计第 6.1 节已同步独立动态首页、六项顶部导航、模块隔离与 B 午夜蓝主题。
- 本轮 50 项相关测试通过，177 页公共资源与章节锚点验证、30 份公共手册行数检查、固定版源码与 CLI 参数证据校验通过；283 页内部测试文档已重建。三份浏览器验收脚本的 Python 语法检查通过，但未实际运行浏览器。
- 本轮结束前仍需继续推进：DFT 学习样板的时序/元件讲解、工程排障交互深度、独立 RTL 对照及真实 OMP Skill/Worker 任务验证。浏览器、CentOS 7 和公司资料相关验收仍未完成。

## Skill 场景检查（说明级评审，不冒充模型实测）

| 场景 | 已交付行为 |
|---|---|
| 目标仓库已有测试框架 | 读取真实 diff，沿用框架/fixture/运行器，验证行为而非镜像实现 |
| 找不到测试框架或环境 | 继续查构建/CI，必要时仅交付测试设计与缺口，不称测试通过 |
| 缺少真实 PAD 库 | 明确单元/端口/模型缺口，可生成核心逻辑，真实 PAD 接入仍未完成 |
| 生成基础电路 | 用户目标工程内生成三级层级、指定接口、普通 DFF/逻辑；不预置 DFT 结构 |
| 请求后续 DFT 扩展 | 只增加明确要求的结构，先读取真实工具/库资料；不猜 Sailor 命令或宣称插入成功 |

两项 SKILL.md 通过 skill-creator 的格式检查；场景行为经过说明级检查，未使用在线模型执行自动评测。

## 未执行的公司环境验证

CentOS 7 现场安装与运行、公司模型服务、内部 docs.db 的真实 schema/索引适配、公司 PAD 模型仿真和 Sailor DFT 插入均未执行。系统兼容性采用用户已确认的前提。Skill/Worker 发现格式依据固定版源码核验，未用实际模型运行团队工作流。公共包不携带公司资料、凭据或内部数据库。

## 二进制维护简化（2026-09-05）

- 更新入口只保留 `scripts/maintenance/maintain_binaries.py`，独立使用标准库，下载时验证发行方 SHA-256、大小和 ELF 架构，两个下载都验证后才替换，临时目录自动清理。
- `dist/` 仅保留两个实体；同步和打包从 ELF 读取架构，网站不再读取 dist 或要求二进制版本与手册一致。移除 AST 更新脚本及其依赖，已有许可声明位于 `licenses/omp/`。
- `tests.test_binaries`、`tests.test_sync`、`tests.test_metadata` 共 27 项：24 项通过，3 项因 Windows 平台或符号链接权限跳过。覆盖 x64/arm64、重复更新不留状态、第二个下载损坏或中断时保留两个原文件、发行方校验冲突、架构错误及现有同步和公共文档边界。
- 公共网站重建及 `tests/verify.py` 通过：177 页链接、资源、公共输入哈希及两份 ELF 实体检查。此次未运行浏览器或 CentOS 7 环境验证。

## Scan 状态联动与可计算排障（2026-09-05）

- Scan 的模型、控制和渲染职责分开；支持全部四位输入、按输入计算无故障期望、逐沿前后状态、历史回看和元件采样来源讲解。回看暂停且不推进/改写模型，继续操作从最新状态前进。SO 标注为本沿采出的旧末级位，不与沿后引脚电平混淆。
- 新增 `tests/test_dft_scan.py`：Python 整数/真值参考逐沿核对全部 16 组输入 × 2 个故障场景，覆盖默认响应、未激活故障、快照隔离、重复复位及非法输入；不是 RTL/门级仿真。12 组刺激激活此单一 D3 stuck-at-0，4 组不激活，此计数不是真实设计覆盖率。
- 工程排障页不再逐条展示预设结果：四个独立小模型计算扫描断点、同步复位、XOR 的 X/mask 和捕获 setup 条件，并保留实际条件对应的证据。取证完成仍可能有异常或无法判定；条件改变清除旧记录。未读取真实工程或调用 Sailor。
- 52 项相关检查通过：Scan 4 项、排障 10 项、其余课程/模型与页面呈现检查；纯 DOM 覆盖输入修改、回看与继续、元件说明、未激活故障、排障条件重算及结束后异常保留。
- 公共门户已用 `--public-only` 重建 177 页，本轮构建不读取黄区；公共资源/搜索/片段、30 份 Markdown 行数及二进制实体检查通过。新版浏览器视觉、实际 HDL 仿真及公司环境验证仍未执行。OCC/JTAG 等其他主题的时序/状态联动仍可继续完善。

- 新入口已实际联网运行成功：OMP `v18.1.10+fork.172`、Claude Code stable `2.1.236`，Linux x64，发行方 SHA-256、大小及 ELF 架构均通过校验。首次查询网络超时后重试成功；下载临时目录已自动清理，dist 仍只有两个实体。未执行这两个 Linux 客户端。
- 对应真实浏览器验收用例已补充到 `tests/test_dft_portal.py`，本轮仅检查 Python 语法，没有启动浏览器。公共打包白名单已包含新增 Scan 模型测试。

## OCC 与 JTAG 的逐沿/逐拍学习（2026-09-05）

- OCC 从总数展示改为源时钟沿驱动：Shift 逐沿透传；Capture 只放行 launch/capture 两沿，随后源沿被计数门控阻断。支持单步、播放/暂停、速度和历史回看；控制条件变化会重置记录。缺功能时钟源时不生成输入或输出沿。圆点和表格是事件/计数表示，不是实际脉宽、频率或无毛刺波形。
- JTAG 明确一拍含上升沿和下降沿：上升沿捕获/移位并迁移状态，进入 Update 后在同拍下降沿提交。厂商时序依据为 Microchip DS31400 第 9.2–9.3 节，链接已写入公共文章；不使用该芯片的 IR 宽度或编码。16 个节点展示全部 TMS 分支，可只读查看说明；逐拍记录与 TAP 高亮、寄存器、指令及讲解联动。
- 修正引脚比较状态：未加载 EXTEST 不产生期望/实际测试结果；改变刺激、故障或提交指令/驱动后清除旧比较。生成 IR 序列保留所选引脚条件，不悄悄消除故障。比较按钮仍明确为独立数字实验，不冒充执行完整 DR/ATE 协议。
- 59 项相关测试通过。新增覆盖 TAP 32 条状态迁移、任意状态五个 TMS=1 复位、进入 Update 的同拍提交、Pause 保持/退出 Shift 仍移位、记录隔离；OCC 32 组布尔控制、计数关闭、缺源沿、单个 launch 不算完整捕获及非法计数。纯 DOM 验证历史回看不推进、继续从最新状态、播放速度及条件重置。
- 已用 `--public-only` 重建 177 页；链接、资源、搜索、实体二进制及 30 份公共 Markdown 行数检查通过。浏览器验收用例已补充，但仅检查语法，没有启动浏览器；新版视觉与公司环境仍未验收。WSL 只读探测未找到 iverilog/verilator，本轮没有运行 HDL 仿真。
